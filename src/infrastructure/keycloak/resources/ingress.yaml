apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak-user-ingress
  namespace: keycloak
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: cluster-issuer
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - accounts.ecoma.io
      secretName: accounts-ecoma-tls
  rules:
    - host: accounts.ecoma.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: keycloak-service
                port:
                  number: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak-admin-ingress
  namespace: keycloak
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: cluster-issuer
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - keycloak.ecoma.io
      secretName: keycloak-ecoma-tls
  rules:
    - host: keycloak.ecoma.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: keycloak-service
                port:
                  number: 9000